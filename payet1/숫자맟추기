mport random

def number_guessing_game():
    """
    1부터 100까지의 숫자를 맞추는 게임입니다.
    총 10번의 기회가 주어집니다.
    """
    secret_number = random.randint(1, 100)
    max_attempts = 10

    print("--- 숫자 맞추기 게임에 오신 것을 환영합니다! ---")
    print("저는 1부터 100 사이의 비밀 숫자를 하나 생각했어요. 맞춰보세요!")
    print(f"기회는 총 {max_attempts}번 주어집니다.")

    for attempt in range(1, max_attempts + 1):
        print(f"\n[{attempt}번째 시도]")
        try:
            guess = int(input("숫자를 입력하세요: "))
            
            if guess < 1 or guess > 100:
                print("1부터 100 사이의 숫자를 입력해 주세요.")
                continue

            if guess == secret_number:
                print(f"🎉🎉🎉 빠바밤! 정답입니다! 🎉🎉🎉")
                print(f"축하합니다! {attempt}번 만에 맞추셨어요!")
                return
            elif guess < secret_number:
                print("더 큰 숫자예요.")
            else:
                print("더 작은 숫자예요.")
        except ValueError:
            print("올바른 숫자 형식이 아닙니다. 다시 시도해 주세요.")

    print("\n--- 게임 종료 ---")
    print(f"에휴 이것도 모르냐 ㅋㅋㅋ")
    print(f"정답은 {secret_number}이었어요.")

# 게임 시작
if __name__ == "__main__":
    number_guessing_game()